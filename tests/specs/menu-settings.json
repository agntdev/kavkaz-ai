[
  {
    "name": "Settings",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Toggle preferences: message language, history retention, compact/expanded replies"
            }
          }
        ],
        "send": {
          "callback": "menu:settings"
        }
      }
    ]
  }
]
